[package]
name = "blackfox-memtest"
version = "1.0.0"
edition = "2024"

[[bin]]
name = "memtest"
path = "../src/memtest.rs"

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ["cfg(bios)", "cfg(uefi)"] }